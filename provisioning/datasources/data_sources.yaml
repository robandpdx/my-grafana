apiVersion: 1

datasources:
  - name: grafana-github-datasource
    uid: github123
    type: grafana-github-datasource
    typeName: GitHub
    access: proxy
    secureJsonData:
      accessToken: ${DS_GITHUB_TOKEN}
